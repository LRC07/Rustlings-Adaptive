#[test]
fn eyeball_table() {
    let src = "| 维度 | 用户解 | 参考解 |\n|---|:---:|---|\n| 行数 | 12 | 10 |\n| 惯用性 | **4/5**，可用 `map` 一次完成 | 5/5，迭代器链 |\n| 备注 | 这是一条特别长的备注需要折行处理看看列宽是否保持对齐 | — |";
    print!("{}", crate::cli::md::render(src, 60, true));
}
