/target
/exercises/.progress
# Local-only files that hold the user's API key (R3)
/config.toml
/.env
# User's exported conversation records (personal data, never commit)
/sessions/

# Generated exercises are user-local runtime artifacts (M3.2)
/exercises/generated/
/exercises/lib_generated.rs

# Course slides (user-provided teaching material, not project artifacts)
/slides/
rustlings_official/

# Course OJ practice pages (user-provided material)
/practice/

# Dev-process materials (kept local; design-doc PDF & AI history go to
# the course platform, not the repo — see assignment deliverables)
/agent/
/docs/
/AGENTS.md
