# 开发复盘与交接（M4.5–M4.8，9.4）

> 对象：M4.5 出题四步（规划/a 组织/b 质量门/c 分层出题/d 模板扩容）
> + M4.6 多模型档案 + M4.7 出题可靠性 + M4.8 提示分级。
> 全部完成并 commit（31dee41 → 58516bd），146 测试全绿。
> 下一个里程碑：**M5 评审门 + 交互式复盘**（README 状态表 ⬜）。
>
> 本文给新 session 三样东西：做了什么、超时问题的完整证据链与
> 候选方案（**重点**）、M5 的入口提示。

## 一、M4.5–M4.8 做了什么

| 里程碑 | 内容 | commit |
|---|---|---|
| M4.5 规划 | 题目四象 + 七条约束（出题规划_M4.5.md §2）+ 组织/生产方案 | 31dee41 |
| M4.5a | 题目组织：`exercises/generated/index.json` 单一事实来源（概念/难度/来源/触发语境/attempts/status/last_error/feedback/hints）+ 做题三层分区（本会话/按主题/全库）+ 种子题 fixture 化 + 对话耦合（题卡/reason 触发语境/[a] 问教练/状态回流 system prompt） | 1c70110 |
| M4.5b | 质量门升级：**首错误码匹配**（未完成模板编译失败时首码 ∈ error_codes）+ 模板 schema v2（confusion 必填 / anti_patterns） | ea4c4ff |
| M4.5c | **分层出题**：ExerciseDraft 统一表示 → `generator::gate_draft` 单一质量门 → L1 直配 / L2 改编（keyword 骨架）/ L3 自由生成（修复环 ≤4 轮，逐轮拒绝原因透出）+ verifier 全链路超时硬化 | e3669ab |
| M4.5d | 模板扩容批次一：12 主题 × 2 档 = **24 个新模板**（总 36），taxonomy 扩至 **52 节点**；repo fixture 升级为**全轮转质量门**（每个槽位轮转值都过完整 gate） | 4063dc |
| M4.6 | 多模型档案：config `[[models]]` + `/model <名>` 一键切换（写回顶层+重建 client）；UI 修复（结果页不清屏、CJK 列对齐、进度行去重） | c96d2c7, 57d9240 |
| M4.7 | 出题可靠性：draft `max_tokens=3000` 硬限（`ChatTurnCaller::chat_turn_bounded` 默认方法，截断 finish_reason=length 反馈"压缩重出"）+ 修复环 420s 耗时预算 + 失败 fallback 收口（结构化指令**禁止教练自行出题**）+ mode 收回程序（公开入口固定 auto） | 1d49656 |
| M4.8 | 活跃配置自动入册（隐式档案，任何状态可切回）；提示分级：36 模板题面去技术提示 + hints 字段全链路 + 做题页 `[h]` 逐级提示 → `[a]` 问教练兜底 | b03fe4c, c594f70, 58516bd |

## 二、做对的决策

1. **规格先于生成**（七条约束 + 规格卡先行）：M4.5d 产稿时 C1 门抓到
   4 处题面/参考解错误（缺 Ok 包裹、move 出 &Tag、deref 强转误解、
   测试数据错误）——真实执行为据的价值持续验证。
2. **单一质量门收口**（gate_draft）：三级出题共用一份门代码；M4.7 的
   max_tokens/预算都加在环上而不是门上，门从未被绕过。
3. **首错误码检查从第一天就抓真问题**：M4.5b 落地即修正 3 个模板的
   error_codes 声明不一致（未完成态的真实首错与声称不符）。
4. **fixture 全轮转质量门**：把 M3"期望值必须由槽位计算"的教训从纪律
   升级为回归测试（每个轮转值过完整 gate，36 模板 × 4 轮）。
5. **失败降级设计化**（M4.7③）：出题工具失败 → 结构化 fallback 指令，
   杜绝了"模型即兴编题绕过质量门"的试用反馈问题。

## 三、踩过的坑（均已闭环）

| 坑 | 根因 | 对策 |
|---|---|---|
| 题面当说明书（试用反馈） | 模板 TODO 把 API 组合/约束名直接写出，easy 题变抄写题 | M4.8：36 模板题面清理 + hints 分级字段 |
| /model 输出"看不见" | 结果页被视口重绘清屏（照抄了 Generate 分支的 repaint） | 结果页约定：/model /usage /topics 一律不重绘 |
| 切换模型是破坏性的 | 顶层活跃配置没有档案身份，切走即丢 | M4.8⑤ 活跃配置自动入册 |
| 模板 body 行数下限 | 删提示行后 body <10 行被规则过滤拒绝 | 补场景注释（场景说明不是提示，属题面） |
| TOML 基本字符串里的 `"` | hints 文本含 format!("{e}") 未转义 | 文案避开双引号；生成脚本要转义 |
| raw string 提前终止 | 测试 JSON 含 `"#`（`#[cfg(test)]` 前的引号） | r##"..."## |

## 四、★ 超时问题专题（最重要的待解决项）

### 4.1 实测证据链（按时间）

1. paratera 端点（GLM-5.3-Flash）：**短请求（~500 tok 输出）137s 完成
   → ~4 tok/s**，且延迟波动极大（同一请求 140s–500s+）。
2. L3 自由生成的 draft 输出失控：**5.6k / 7.2k / 16.2k tok**（合理体积
   2–3k）——纯 prompt 指令"压缩到 2800 tok"不可靠。
3. 换更快的档案（DeepSeek-V4-Flash / Kimi-K3，同为 paratera 域名）后
   **仍卡在 ~120s**——强烈疑似**网关对非流式请求的硬超时**
   （nginx proxy_read_timeout 120s 是业界默认），不是生成不完。
4. 用户最新实测（M4.7 之后）：**简单 Rc<RefCell<T>> 题约 200s 出题
   成功**——三件套后"勉强能用"，但离顺滑还远。
5. 修复环在慢端点的历史表现：4 轮全超时；逐轮拒绝原因反馈正常
   （max-lines 超限 → 模型改小 → 首错误码不符 → 修正）。

### 4.2 根因分解（乘法模型）

```
总时长 ≈ 轮数 × (排队延迟 + 输出体积 ÷ 吞吐)
```
- 端点侧：吞吐低 + 排队长 + 疑似非流式网关 120s 掐断（换端点无效的
  现象与此吻合）。
- 协议侧：整题 JSON 一次输出（body+tests+reference+转义膨胀 ≈ 2.5–5k
  tok 合理体积）→ 任何慢端点都要 100s+ 单请求。
- 工程侧已缓解：max_tokens 硬限（教育收敛）、420s 预算止损、失败
  fallback、多档案快速切换、逐轮拒绝原因可见。

### 4.3 候选方案（推荐顺序）

1. **分步生成（主推，保险预案已写入设计文档 §8 M4.8）**：把整题 JSON
   拆成 3 个小请求——
   - 步 1 设计卡：场景/根因/错误码/约束选择（~300 tok）
   - 步 2 题面 body（~600 tok）
   - 步 3 tests + reference（~800 tok）
   每步输出 <1k tok，**在最慢端点也能 <120s 完成**；顺带绕开网关
   超时（每步都短）、提升首轮成功率（每步任务简单）、体积天然受控。
   程序在步间组装 + 现有 gate_draft 收口不变。工作量 ~1 天。
2. **流式（stream）请求**：不改变总时长但保持连接有数据流动，绕开
   非流式网关超时。llm client 改 SSE 解析，成本中等。若分步后仍见
   120s 整点失败再做。
3. 治标补充：慢端点下调修复环预算（420s → 240s）+ 出题前在对话里
   提示"该端点较慢，预计 2–5 分钟"。
4. 模板库持续扩容（第 1 级命中率越高，越少走 L3）——批次二剩 7 题
   （闭包三态/Rc-RefCell/Deref/HashMap/悬垂已并入 12b/Result 剥层/
   模块可见性）。

### 4.4 验证方法（新 session 直接可用）

用 `/g <主题>` 直连路径（不走 agent 环）计时；观察逐轮 note；对比
`/model` 切换不同档案；看 `exercises/generated/index.json` 的 attempts。
若出现"120s 整点失败"即网关超时实锤 → 优先做分步生成或流式。

### 4.5 验证结果（9.4 晚真实端点冒烟，M4.10/M4.11 会话执行）

> 总花费 $0.043（147→154 次调用）。L1/L3 各跑一次 + M4.10 去重验证。

1. **M4.7 三件套实测**：
   - 修复环 live 工作：L3 第 1 轮被质量门拒（实际首错 E0594 ≠ 声明的
     E0525——门抓到真实不一致）→ 拒因反馈 → 第 2 轮通过 ✓
   - fallback 收口未实测（需失败场景，单测已覆盖）
2. **M4.8 hints 实测**：L1 模板题 3 条 ✓；L3 自由生成**原为 0**——根因
   = draft prompt 输出格式没要求 hints 字段（M4.11 修复后 3 条 ✓）
3. **★ 120s 网关超时：今天未复现**（吞吐 ~150 tok/s vs 9.4 的 ~4 tok/s），
   L3 端到端 2m40s/$0.0125 与 6m10s/$0.0303——分步生成的紧迫性下降，
   是否还做**等更慢端点实测再定**
4. **★ 新发现：该端点忽略 max_tokens**——请求确定携带 3000（代码路径
   唯一 + 单测），响应仍 19.5k/21.9k/27.5k completion tok，而题面本身
   很紧凑（门全过）→ 大概率是端点侧隐藏推理 token 计入 completion。
   这同时解释了 §4.1"输出体积失控 5.6k–16.2k"的老谜团：可见内容可能
   从来不大，失控的是推理 token。**推论**：prompt 侧"压缩体积"指令从来
   不可能有效（压的是可见部分）；真正的杠杆是换无推理/少推理的模型或
   关闭思考模式（config think_mode 字段仍是预留）

### 4.6 探测定案（M4.12，live probe 三连，evidence 存 llm/mod.rs 测试注释）

1. DeepSeek V4 Flash 是混合推理模型：**默认开思考，effort 默认 high**；
   控制参数 `{"thinking": {"type": "enabled"/"disabled"}}`，另有
   `reasoning_effort`（low/high/max）。
2. **max_tokens 不约束思维链**（只管最终回答）：max_tokens=32 + 诱导题
   → completion=54（其中推理 52）、finish_reason=stop 自然结束。
   → M4.7 的 max_tokens 硬限对思考模型**只限可见答案，永远兜不住总
   体积**——"输出失控"之谜正式破案。
3. `thinking=disabled` 立即生效：同题 completion 1 tok、0 推理。
4. **落地（M4.12）**：config `think_mode` 三态 auto/on/off（兼容旧
   bool：false→auto，true→on）→ `LlmClient.with_thinking` → 请求体；
   usage 解析 `completion_tokens_details.reasoning_tokens`，回合
   footer / /usage / 出题 LLM 行均显示"（其中推理 X）"。
   **用户侧动作：`/config` 选 6 或 config.toml 改 `think_mode = "off"`。**
5. 已知限制：官方 DeepSeek 端点要求"带 tools 的请求把
   reasoning_content 传回，否则 400"——当前网关未强制（工具环实测
   正常），未实现回传；若换官方端点遇 400，先设 think_mode = "off"。

### 4.7 推理档位实验（M4.12，9.4 晚；L3 draft 层直测，主题 =
Rc<RefCell<T>> 共享可变状态与运行时借用——即 session 20260904_202243
四轮全败的同一题；复跑脚本 = generator::live_draft_effort（#[ignore]））

| 档位 | 结果 | 轮数 | 耗时 | 输出 tok（推理） |
|---|---|---|---|---|
| off | PASS（压线） | 4/4 | 26s | 2,800 (0) |
| off | **FAIL** | 4/4 | 25s | 3,039 (0) |
| on+low | PASS | 1 | 71s | 8,502 (7,736) |
| on+low | PASS | 4 | 204s | 24,180 (21,539) |
| on+high（早前冒烟） | PASS | 1–2 | 160–370s | 19.5k–27.5k |

结论：
1. **off 快但不可靠**——成功率 ~1/3（含用户会话），失败的 4 轮全是
   纯浪费；模型无自检，常死在"参考解编译失败/概念超限"这类低级坑。
2. **on+low = 平衡点**——4/4 全过，平均耗时低于 on+high。但注意
   effort 只是请求档位，run2 的 21.5k 推理说明模型仍可自行多想；
   DeepSeek 无推理 token 硬上限参数（"设置推理上限"在该 API 不可行，
   effort 是唯一杠杆）。
3. **结构性优化**：任务越小推理越短——分步生成（§4.3 方案 1）与
   reasoning_effort 是互补的两个杠杆，前者仍值得做。
4. **推荐用户配置**：`think_mode = "on"` + `reasoning_effort = "low"`。

### 4.8 三模型推理档位对照（9.4 晚查证；M4.14 起档案级可配）

| 模型 | 思考开关 | 档位 | 默认 | 坑 |
|---|---|---|---|---|
| deepseek-v4-flash | `thinking.type` on/off | `reasoning_effort` low/high/max | 开 + high | max_tokens 不限思维链（§4.6） |
| kimi-k3 | **恒开，不可关** | `reasoning_effort` low/high/max | **max**（最贵档！） | K3 请求 schema 无 thinking 参数；另 max_completion_tokens 默认 131k |
| glm-5.3-flash | **仅能 enabled**（5.3 起） | `reasoning_effort` low/high/max | **max** | 早期 GLM-4.5 文档无 effort，勿混淆；5.3 思考深度全由 effort 控制 |

结论：三家档位语义不一 → M4.14 把 think_mode/reasoning_effort 下放到
`[[models]]` 档案级（留空沿用当前值）。三家 effort 参数名与本项目
线上一致（顶层 `reasoning_effort`）。

### 4.10 实机三模型横评（M4.15，9.4 深夜；同任务脚本：答疑 E0502 + mode=free 出 RefCell 题）

| 模型/档位 | 答疑 | mode=free 出题 | 题面质量 | 出题成本 |
|---|---|---|---|---|
| glm-5.3-flash @ **high** | ✓ 正常 | **✗ 4 轮全败**（参考解编译失败 E0596） | — | 高 |
| deepseek-v4-flash @ low | ✓（推理 41 tok） | ✓ 1 轮过，1m33s | 考点对，**题面泄漏解法** | $0.0047 |
| kimi-k3 @ low | ✓（推理 11 tok） | ✓ 1 轮过，1m45s | **干净、场景真实、C5 合规** | **$0.0019** |

结论与调档建议：
1. **推荐默认 kimi-k3 @ low**（K3 官方默认 max，必须显式配 low）；
2. **glm 档建议 high → low**：live 实验（§4.7，同为 GLM）on+low 4/4
   全过，on+high 实测 4 轮全败——high 档对自由生成无益且更贵；
3. deepseek @ low 可用，作备选；
4. 质量门全程未放行坏题（GLM 全败 = 诚实报错，无坏题泄漏）✓；
5. 新发现 backlog：**L3 生成题的题面会泄漏解法**（DeepSeek 题 TODO 注释
   直接写出 borrow_mut 路径）——手写模板 M4.8 清理过，生成题无此检查；
   候选修法 = 规则门加 C5 机器可查版（题面与参考解核心 API token 比对），
   与 M5 评审门一并考虑。
6. 观察项（非 bug）：GLM 在"裸粘贴后追问"场景出现上下文失忆
   （[14]–[21]），history 完整、window 无裁剪——模型连贯性问题，
   M5 的 open-loop 注入可缓解。

### 4.9 用户实测两痛点与修复（M4.14，session 26/27）

1. **"继续生成"被强行引导回模板**（session 26：要 RefCell 运行时借用
   → 连续两次落进相邻模板）：根因 = M4.7 把 mode 完全收回后，教练没有
   通道尊重用户"不用模板"的明确要求。修复 = 受限回归：工具 mode 只有
   auto|free 两值，**auto 仍是默认**（M4.7 的"策略程序定"不变），free
   仅是用户意图的转达（跳过 L1/L2 直进 L3，照过同一质量门）；系统提示
   写明"多次模板错配是建议 free 的信号"。
2. **工具轮次耗尽被硬切、零结论**（session 27：5 次 check_code 实验
   全败 → 罐头文本收场）：修复 = 轮次用尽后追加一条"立即基于已有诊断
   给出结论"的指令并做**最后一次无工具调用**，保证用户总能拿到结论；
   系统提示同步加实验纪律（check_code 连败两轮即停止实验、直接解释）。
5. **顺带抓到的真 bug（M4.11 修复）**：`/g` 直连路径的 LLM caller 是裸
   闭包，`call_bounded` 走默认实现**直接忽略 cap**——即 M4.7 的硬限在
   `/g` 路径从未真正发出去过（agent 工具路径的 CallerBridge 不受影响）

## 五、当前能力清单（演示脚本素材）

- 对话出题：题卡（概念/难度/触发语境"为什么出这题"）→ 做题三层分区
  → [h] 分级提示 → [a] 问教练（代码+状态带回对话，rustc 取证）
- 三层出题：/g 任意主题 → auto 降级（36 模板直配 → 改编 → 自由生成），
  每题过同一质量门（三重校验+约束自洽+首错误码）
- /model 多档案切换（含活跃配置入册）、/usage 计费、预算拦截
- 做题状态机（✗n 累计/Passed 粘滞/last_error）落 index.json，
  教练 system prompt 感知练习进度

## 六、试用反馈（9.4 晚，实测会话诊断）

> 用户用多档案实测后反馈三问题，已对照会话轨迹（session_20260904_
> 172430）逐一定性。**均未修复，记录为待办**。

### 6.1 粘贴多行被逐行发送（高频痛点）

- 现象：粘贴约 20 行函数，被按行拆成 **9 条独立消息、9 个 LLM 回合**；
  教练回复 6 次「还差…/你没贴上来」直到片段凑齐（会话消息 [1]–[19]）。
- 现有缓解：``` 围栏粘贴模式（两行 ``` 之间合为一条）——但自然粘贴
  不带围栏，用户不会主动用。
- 修法候选（推荐组合）：
  1. **bracketed paste**：现代终端粘贴自带 `ESC[200~ … ESC[201~` 包裹，
     `cli/input.rs` 的 raw-mode 编辑器可识别并聚合为一条输入（终端
     原生语义，治本）；
  2. **时间启发式兜底**：无包裹时按「行间到达零停顿」聚合（粘贴流
     行间隔 <10ms，人手打字不可能）；
  3. 教练侧无法根治——回合是 CLI 输入层发起的，聚合必须做在输入层。
- **延伸（UI 顺手度 backlog）**：readline 方向键历史（M4.2 曾评估
  不做，bracketed paste 落地后可重估）、输入模式可见性（做题页 vs
  对话页的提示恒显）、长输出页（/model /sessions）的分页体验。

### 6.2 出题匹配粒度：同概念域 ≠ 同考察点

- 现象：用户刚与教练对练「unwrap_or vs unwrap_or_else 急切/惰性求值
  （白分配问题）」，说「出个题吧」；教练 topic 正确指向该考察点，
  但 L1 的 LLM 选了 option-and-then-chain（**and_then+map 链式改写**，
  同属 types.option-result 但考察点不同）。用户「感觉你要考的不是
  这个」，教练在后续轮次承认错配（会话 [38]–[45]）。
- 根因：题库无「unwrap_or vs unwrap_or_else 求值时机」模板；LLM 选
  模板的 no_match 判据太松（同概念域即视为相关）。
- 修法候选：
  1. `llm_pick_template` 的 prompt 收紧：**必须训练用户要求的具体
     手法/行为，同域不算匹配** → no_match 走 L2/L3（自由生成恰好
     适合出这类精确对题的题）；
  2. 模板批次二新增主题：「unwrap_or/map_or 与 *_else 变体的急切 vs
     惰性求值」（正好衔接课程与本次实测）；
  3. 出题工具的返回值带**匹配依据**（哪层/为何选此模板），教练发现
     错配时应**主动重新出题**而非道歉了事。

### 6.3 对话内悬而未决任务无跟进（M5 复盘交互必须覆盖）

- 现象：教练帮用户改写代码后，用户逐个贴改写验证（这部分正常，每条
  都 check_code）；但话题一转开（出题），教练**不会主动回访**之前
  未完结的代码任务（「离开那个对代码提问后他就不会自己去看了」）。
- 现状：练习状态有回流（index → system prompt 的 practice_note），
  但**对话内的 open loop**（贴过的代码、待验证的改写、用户未答的
  问题）没有任何注入——全靠模型「自觉」，而它不会。
- 修法候选：仿 practice_note 加「对话 open-loop 摘要」注入（最近
  讨论的代码任务/待验证项）；system prompt 加跟进原则（话题转开前
  有未验证改动时主动询问回访）。**M5 的 follow-up 决策设计时一并
  落地，不要单做。**

## 七、下一步（M5 交接，更新于 9.4 深夜 M4.15 后）

**M0–M4.15 全部完成**，161 测试全绿。下一个里程碑：**M5 评审门 +
交互式复盘**（设计文档 §4.2/§4.3 已定稿，README 状态表 ⬜）。

M5 的入口与可复用件：
1. **评审门**（§4.2）：静态检查 + LLM 逻辑评审（clean/suggestions/
   suspicious）。接线点 = 做题通过后（`practice.rs` 的 Passed 转移处）；
   静态部分复用 `constraints::check`（todo! 残留/clippy/行数）；LLM 评审
   的输入素材 = 模板/draft + 用户解 + 测试结果（Outcome/VerifyReport
   都在调用链上）。
2. **复盘四步**（§4.3）：hints 分级（M4.8）与 `[a]` 问教练是自然入口；
   解释校核的选择题/自由输入、更优解挑战、双维度对比表、follow-up
   决策（Variant 机制 M4.10 已铺好——同概念换槽位/换值）。
3. **§6.3 open-loop 注入**：仿 practice_note 加"对话 open-loop 摘要"
   （最近贴过的代码/待验证改动），随 follow-up 决策一并落地，不单做。
4. **usage/ phase 标签**：评审/复盘单独计费可见（`record` 已带 phase
   参数，加 "review"/"debrief" 即可）。
5. **交接时的 backlog（按优先级）**：
   - L3 题面泄漏解法（§4.10⑤）——C5 机器可查版规则门；
   - 模板批次二 7 题（闭包三态/Rc-RefCell/Deref/HashMap/Result 剥层/
     模块可见性/unwrap_or 求值时机）——提高 L1 命中，减少 L3 依赖；
   - 分步生成（§4.3 方案 1）——若慢端点实测失败率高再做；
   - GLM@high 自由生成不可靠（§4.10）——建议用户改 low，产品侧无动作。
6. **当前推荐配置**（写进展示帖/README 均可）：kimi-k3 @ low（又快又省
   又好）；deepseek @ low 备选；glm @ low（勿用 high）。
7. 测试期间的副产品：三模型横评脚本可复现（piped `/model X` + 答疑 +
   free 出题 + `/exit`），演示时可直接用作"多模型对比"环节。
