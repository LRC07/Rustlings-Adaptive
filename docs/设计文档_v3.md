# RustLings-Adaptive 设计文档（v3）

> 相对 v2 的变更（本轮迭代吸收的想法已逐条落位）：
> 1. **知识锚点改双轨**：错误码（E0xxx）保留为"粗轨"——可靠但粒度粗；
>    新增**细分概念图谱**作"细轨"（`taxonomy/concepts.toml`，机器可运营）。
>    信号源从单一编译诊断扩展为**五类**（编译诊断 / 测试失败 / 修复模式 /
>    clippy / 复盘表现）。背景：用户本地有 rust-analyzer，编译错误常在
>    跑到 CLI 之前就被修掉，单靠 E0xxx 会漏信号；"测试全绿"也不等于改得好。
> 2. **新增解答评审门**：测试通过后，LLM 对"模板 → 用户解"做逻辑评审，
>    从失败测试与非最优改法中提取信息（多余 clone、手动循环代替迭代器、
>    硬编码期望值等），据此决定复盘形态。
> 3. **复盘改交互式分支**：原理解释支持**选择题或自由输入**；评审门发现
>    更优解时给方向性提示，用户可再试或查看解答；对比总结后记录掌握度，
>    并由 Agent **决定是否再出一道相关题**（变式 / 加约束 / 升难度）。
> 4. **题目模型新增抽象约束**（constraints）：如 no-clone、iterators-only、
>    max-lines，机器可校验的部分由静态检查硬判，作为"设计习惯训练"载体。
> 5. **定位强化"小片段主义"**：项目的亮点是把复杂场景规约到一个个小的
>    片段，质量易把握、便于针对性指导与统计数据；P2 新增"真实代码片段
>    → rustling"的规约管线。
> 6. 题目模板格式定为 TOML；槽位规格给出最小 schema（未决项收窄）。

## 1. 产品定位

一个**对话式 Rust 诊断教练** Agent。用户在 CLI 中用自然语言提问/贴代码，
Agent 解释、必要时**在对话中即时插入一道针对性填空题**作为验证；题目不强
求独立构成完整题库，只要求"与当前上下文相关、能暴露薄弱点、机器可解"。

- **给谁用**：正在学 Rust 的开发者（含本课程同学），卡在所有权/借用、
  生命周期、泛型、trait 等编译期概念上的人。
- **解决什么**：rustlings 固定无个性化；通用 LLM 能解释但不会本地验证、
  不记错误、无法在对话中即时出可练的题。本 Agent 把"解释 → 验证 → 诊断
  → 复盘 → 归档 → 定向巩固"做进同一对话。
- **教学立场**：不只补语法漏洞，更借小片段培养设计习惯——直接改大段真实
  代码难以下手，但确定的 rustling 场景小而可控，适合从一个个小模块中学
  习更好的设计思路。

### 1.1 设计原则：小片段主义

1. **小片段优先**：题目 10–40 行、聚焦 1–2 个概念、机器可判。复杂场景
   一律先规约成小片段再教学——这是全项目的核心方法论，也是质量可控、
   统计可行的前提。
2. **真实执行为据**：解释、出题、评审、复盘都必须有 rustc / 测试 / 实测
   数据背书，不靠模型主观判断。
3. **一切信号落数据**：每次交互产出结构化记录（五类信号），单次稀疏也能
   逐步积累成画像。

## 2. 目标用户与痛点

| 痛点 | 现状 | 通用 Agent 为何解决不好 |
|---|---|---|
| 同类错反复犯 | rustlings 不记错误 | 无持久状态 |
| 解释完不知是否真懂 | 用户"我懂了"不可信 | 无可执行验证手段 |
| LLM 给的题常不可编译 | 直接生成代码带错 | 不本地跑 rustc |
| 借用报错看不懂 | 模型从文本猜常错 | 无真实编译器还原因果 |
| 本地有 rust-analyzer，编译错误在 CLI 前已被修掉 | 编译错误码采集不到 | 信号源只有编译诊断一条路 |
| 错误码粒度太粗，同一码下短板各异 | E0277 下可能是十种不同错 | 无细分概念体系，锚点不精确 |
| 测试全绿 ≠ 改得好 | 多余 clone、手动循环等非最优改法无人指出 | 不做本地逻辑评审 |
| 改大段真实代码无从下手 | 缺小步、可对照的练习载体 | 无"小片段 + 约束 + 多解对比"机制 |

根本原因：通用 Agent **既缺持久状态，也无法可靠本地执行与校验代码**。

## 3. 场景定制（通用 Agent 做不到的事）

1. **rustc 即工具 + 三重校验**：任何要给用户的题（模板或生成）必过本地
   `rustc --test` 三重校验（能编译 / 参考解全绿 / todo 模板必失败）。
2. **双轨知识锚定 + 多源信号**：细轨是细分概念图谱（一个概念 = 一个能用
   10–40 行片段孤立考察的点，如 `borrow.split-fields`、
   `traits.assoc-type-vs-generic-param`），粗轨是 rustc 错误码，二者互为
   校验（模板标注码与真实不符 → 反哺修正）。信号源五类：编译诊断、测试
   失败、修复模式（用户解与模板的 diff 特征）、clippy lint、复盘表现。
   全部归一为带权重的 Signal 入画像。
3. **解答评审门**：测试通过 ≠ 结束。静态检查（todo!/unimplemented! 残留、
   约束、clippy）+ LLM 逻辑评审（题面 + 模板 + 用户解 + 测试结果 →
   clean / suggestions / suspicious），从失败测试与非最优改法中提取信息；
   suspicious（如硬编码期望值、擦边通过）加探测测试再验。
4. **交互式复盘**：通过后，Agent 复现原失败代码与真实诊断，请学习者解释
   "为什么之前不过、现在能过"（**选择题或自由输入皆可**），据真实错误码
   与根因关键词校核；发现更优解时**给提示让用户再试或查看解答**；最后
   给出实测对比总结（行数 / clippy / 编译时长 / 运行时 / 约束满足），
   记录知识掌握情况，**决定是否再出一道相关题**。
5. **可校验的抽象约束**：题目可附抽象修改约束（no-clone、iterators-only、
   max-lines…），静态可判的机器硬判，其余 LLM 判定 + 实测佐证。约束是
   "逼出更优解"的脚手架，直接服务设计习惯培养。
6. **借用检查器假设实验室（招牌，P1）**：假设性改代码 → rustc → 诊断
   增量（旧 E0xxx → 新 E0xxx），让借用规则因果可观察。
7. **错题本 + 检索闭环**：每条错误归档（错误码、概念、最小复现、修复、
   用户解释、评判）；下次同码/同概念出错检索相似旧错。持久状态 + 检索。

## 4. 核心交互流程

CLI 主体是**对话 REPL**（满足 R2），辅以菜单跳转。

### 4.1 对话诊断（主流程）
```
用户：贴一段报错代码 / 问概念（"为什么这里要 &mut self"）
  → Agent：解释 + 双轨锚定（错误码 → 概念图谱节点）
  → [若需巩固] Agent 调用工具生成/选一道填空题（三重校验后展示）
用户：在 $EDITOR 补全 → [r] 运行
  → Agent：rustc --test 实时结果
      失败：错误码级反馈 + 从失败断言提取"下一步提示"（不直接给答案）
      通过：进入解答评审门（4.2）
```
长任务（LLM/编译 >3s）显示进度，Ctrl-C 打断（R4）。

> **界面定位（定稿）**：启动即进入**对话界面**（REPL 是默认首屏，M4 落地）；
> 类 rustlings 的做题界面是**子模式**，由对话中出题触发或按需进入。
> `exercises/` 现有 8 道种子题的定位是**开发期 fixture 与演示素材**，
> 不构成最终产品的默认界面内容（M8 收尾时决定 fixture 化或移除）。
>
> **编辑器接入（定稿）**：做题编辑不再绑死 vi。解析优先级：
> `$EDITOR` / `$VISUAL` → config.toml `editor` 字段 → 自动探测 VS Code
> （`code --wait <file>`，`--wait` 保证 CLI 阻塞等待编辑完成）→ vi 兜底；
> 配置页 `[c]` 可交互修改。
> 取舍：在 VS Code（rust-analyzer 子 crate）里编辑时，行内诊断直接可见、
> 用户常在保存前就修完编译错误，CLI 侧编译反馈不再是主信息源——这正是
> §3.2 多源信号设计的根据：**细轨概念标签与测试结果比错误码更可靠**，
> 错误码降级为机会性信号（拿到就用，拿不到不依赖）。

### 4.2 解答评审门
```
[测试全绿]
  → 静态检查：todo!/unimplemented! 残留、constraints 静态项、clippy、行数
  → LLM 逻辑评审（题面 + 模板 + 用户解 + 测试结果 → JSON）：
      verdict = clean | suggestions | suspicious
      findings = [{kind, severity, message, better_way}]
  → clean        → 进复盘（解释环节可轻量）
  → suggestions  → 复盘 Step 2 触发"更优解挑战"
  → suspicious   → 自动加一道探测测试再验；仍可疑 → 不计掌握，标记归档
```
非最优改法示例：能用 `&str` 处处 `String`、能拆分借用却 clone 逃逸、
手动 for 循环可写成迭代器链、match 可写成 `matches!`。

### 4.3 交互式复盘（分支）
```
Step 1 原理解释（之前为什么不过 / 现在为什么能过）
  Agent 据真实诊断 + 概念生成 3–4 个选项（1 正确根因 + 2–3 常见误解）
  用户：[数字] 选择 或 直接输入文字
  校核：选择题直判；自由输入由 LLM 对照真实错误码 + 根因关键词判 hit/miss
  （一次通过无失败快照时，改为问"这题考什么 / 模板为什么会失败"）
Step 2 更优解挑战（评审门 verdict ≠ clean 或与参考解风格差异大时触发）
  Agent 只给方向性提示（"这一步可以用迭代器一次完成"），不给答案
  用户：[r] 带提示再改一次   /   [s] 查看参考解与对比
Step 3 对比总结
  对比表分两类维度（定稿）：
  · 机器实测（确定性，不用 LLM 编数）：有效行数 / clippy lint 数（种类
    与个数）/ 编译耗时 / 测试运行耗时（多次取中位）/ 约束满足
  · LLM 评审（机器测不出来的判断，每项给 短评 + 具体改法，可打 1–5 分）：
    惯用性（是否用了语言的标准手法）/ 可读性（命名与控制流清晰度）/
    可维护性与扩展成本（加一种类型要改几处）/ 设计习惯（权限最小化、
    无多余 clone、边界与 panic 风险）
  → 表格（用户解 vs 参考解 ± 更优解）+ Agent 一句话点评取舍
Step 4 归档与决策
  SM-2 更新概念节点（尝试次数 / 是否用提示 / 解释命中 / 约束满足）
  decide_follow_up：
      一次通过 + 解释命中 + clean          → NextConcept（下一概念）
      通过但解释 miss 或用了提示           → Variant（同概念换槽位变式）
      通过但约束未满足 / suggestions       → Variant + 附加约束
      ≥3 次失败                            → 降难度 Variant + 引导看错题本旧错
```

> **LLM 使用原则（定稿）**：能确定性验证的环节不烧 LLM（三重校验、
> 约束检查、行数、clippy 全离线）；机器测不出来的判断——解释、根因
> 校核、可读性/惯用性/可维护性评审、自由出题——放开用 LLM，且要求
> 给出理由与具体改法，不给只打分的黑盒结论。取舍标准是**用户收益**，
> 不是调用次数：省 token 省在不影响效果的地方（如离线填槽回退），
> 该花的地方（评审/复盘/出题质量）花足。

### 4.4 自然触发出题的几种形态
- 用户报错 → 解释后问"要不要做道类似的题巩固"
- 用户连续两次同概念错误 → 主动插入诊断题
- 用户说"我懂了" → 用一道题验证（不信主观判断）
- 测试全绿但评审门 suspicious → 探测测试验证真懂

### 4.5 借用实验室 / 统计
- borrowlab：贴代码 + 假设性改动 → 诊断 diff + 解读
- stats：概念图谱（SM-2）、错题本、token/花费

## 5. 功能清单（按优先级）

**P0（MVP，闭环）**
- F1 对话 REPL + 菜单（扩展现有 CLI）— R2
- F2 模型配置（endpoint/key/价格/上下文/思考）— R3
- F3 token 用量与价格统计 + 预算中断 — R6
- F4 进度渲染 + 打断 — R4
- F5 模板库（10–20 手写，TOML，含概念标签与约束）+ 概念图谱初版
- F6 三重校验（rustc --json 解析）+ 五类信号解析 + 自动入库接线 — R1 核心
- F7 解答评审门（静态检查 + LLM 逻辑评审 + 探测测试）
- F8 交互式复盘（解释选择/输入 + 更优解挑战 + 实测对比 + 定向再练决策）
- F9 双轨画像 + 错题本（带检索）+ 会话历史存 JSON — R5

**P1（招牌）**
- F10 借用检查器假设实验室
- F11 约束静态检查升级（syn 级 AST 判定，替代 token/regex 初版）
- F16 分层出题（M4.5 提级）：模板改编 + 自由生成，同一质量门收口

**P2（设计前瞻，主线不碰）**
- F12 真实代码片段 → rustling 规约管线（harvest 扫真实项目 → simplify
  去项目依赖 → template-ize 挖空加约束 → 三重校验入库）
- F13 重型探针变体解释评分（多探针命中率）
- F14 LLM 模拟试做给难度档位（弱信号）
- F15 诊断最小化（delta reduction）
- F17 项目感知练习、多语言迁移

## 6. 数据源

| 数据 | 来源 | 用途 |
|---|---|---|
| 编译诊断（结构化） | `rustc --error-format=json` | 验证、粗轨锚点、诊断增量 |
| 测试结果（含失败断言 expected/actual） | `rustc --test` 运行 | 评审门、细轨信号 |
| clippy 诊断 | `cargo clippy --message-format=json` | 评审门、多解对比（P0 即用） |
| 用户修复 diff | 模板 ↔ 用户解 | 修复模式信号（中权重） |
| LLM | OpenAI 兼容 API（含 function/tool calling） | 解释、填槽、评审、复盘、解读 |
| 题目模板 | 手写 `templates/*.toml` | 生成基座 |
| 概念图谱 | 手写 `taxonomy/concepts.toml`（repo 内版本化） | 细轨锚点、画像、定向 |
| 错误码释义 | rustc error index（可选抓取） | 解释辅助 |
| 用量与价格 | API usage + 配置价格表 | R6 |

## 7. 技术设计

### 7.1 模块划分
```
src/
├── main.rs                薄入口（M1 前置：现有单文件拆分为模块，纯重构）
├── cli/                   对话 REPL、进度渲染、打断
├── config/                模型配置（.env/config.toml）— R3
├── llm/                   OpenAI 兼容客户端 + tool calling — R1
├── exercise/              练习模型、发现、rustc 运行器（已部分存在）
├── verifier/              三重校验、rustc --json 诊断解析、五类信号提取
├── template/              模板库加载（TOML）、槽位填充、规则过滤
├── taxonomy/              概念图谱加载（concepts.toml）、错误码反查索引
├── constraints/           抽象约束的静态检查（初版 token/regex 级）
├── generator/             模板选择 + LLM 填槽 + 校验重试 + 自动接线
├── review/                解答评审门 + 交互式复盘（解释校核、更优解挑战、
│                          实测对比、follow-up 决策）
├── borrowlab/             借用假设实验室（诊断 diff）— P1
├── profile/               双轨画像（错误码计数 + 概念 SM-2）、错题本+检索 — R5
├── store/                 JSON 落盘
├── usage/                 token/价格统计、预算中断 — R6
└── agent/                 Agent 环：工具定义、调度、轨迹记录
exercises/                 练习仓 + IDE 子 crate（已存在）
templates/                 手写题目模板（TOML）
taxonomy/                  概念图谱（concepts.toml）
```

### 7.2 关键数据结构

```rust
// ===== 概念图谱（细轨锚点，repo 内版本化）=====
struct ConceptNode {
    id: String,                    // "borrow.split-fields"
    name: String,
    parents: Vec<String>,          // 图谱边：上位概念
    error_codes: Vec<String>,      // 常触发码（与粗轨互为校验，多对多）
    fix_patterns: Vec<String>,     // 常见修复模式标识
    templates: Vec<String>,        // 覆盖该概念的模板 id
}
// 用户侧掌握度（SM-2），存 ~/.rustlings_adaptive/concept_state.json
struct ConceptState {
    concept: String,
    mastery: f32, ease: f32, interval_days: u32, last_review: Option<DateTime>,
}

// ===== 五类信号（统一入口，带权重）=====
enum Signal {
    CompileError { code: String, span: Span },               // 强
    TestFailure { test: String, expected: String, actual: String }, // 中强
    FixPattern { kind: FixKind },                            // 中（LLM 从 diff 判）
    ClippyLint { lint: String },                             // 弱-中
    ExplanationMiss { concept: String },                     // 弱
    ConstraintMiss { constraint: String },                   // 中
}
// 权重初值：1.0 / 0.8 / 0.6 / 0.4 / 0.5 / 0.6（可调）

// ===== 题目模板（TOML）=====
struct Template {
    id: String,
    concepts: Vec<String>,         // 细轨概念 id（≤2 个）
    error_codes: Vec<String>,      // 粗轨：此题做错时常触发的码
    difficulty: Difficulty,
    code: String,                  // 含 {{slot}} 占位与 // TODO
    slots: Vec<SlotSpec>,          // kind: ident | type | literal
    tests: Vec<TestCase>,
    reference_solution: String,
    constraints: Vec<Constraint>,  // 抽象修改约束
    review_hints: Option<ReviewHints>, // 根因关键词 + 误解选项种子
}

// ===== 抽象约束 =====
enum Constraint {
    NoClone,                       // 禁 .clone()/.to_owned()      静态
    NoUnwrap,                      // 禁 unwrap/expect             静态
    IteratorOnly,                  // 禁 for/while 循环            静态
    MaxLines(u32),                 //                              静态
    SinglePass,                    // 禁嵌套循环                   半静态+LLM
    CustomStatic { ban: Vec<String> },   // 禁用 token 序列         静态
    CustomJudged { rubric: String },     // LLM 按 rubric 判        判定
}
// 质量门要求：参考解必须满足模板自己声明的全部静态约束（自洽校验）

// ===== 解答评审门 =====
enum ReviewVerdict { Clean, Suggestions, Suspicious }
struct SolutionReview {
    verdict: ReviewVerdict,
    findings: Vec<Finding>,        // {kind, severity, message, better_way}
    probe_test_added: bool,        // suspicious 时加探测测试再验
}

// ===== 交互式复盘 =====
enum Explanation {
    Choice { options: Vec<String>, answer: usize, pick: Option<usize> },
    Free { text: String },
}
struct BetterSolutionChallenge {
    hint: String,                  // 方向性提示，不含答案
    retried: bool,
    final_code: Option<String>,
}
struct ReviewRound {
    failing_snapshot: Option<String>,   // 最后一次失败代码（一次过则 None）
    diagnostics: Vec<Diagnostic>,
    explanation: Explanation,
    explanation_hit: ExplanationCheck,  // 是否命中真实错误码/根因
    challenge: Option<BetterSolutionChallenge>,
    comparison: ComparisonTable,        // 机器实测（行数/clippy/编译时/运行时/约束）
                                        // + LLM 评审（惯用/可读/可维护/习惯，§4.3）
    follow_up: FollowUpDecision,        // None | Variant | Harder | NextConcept
}

// ===== 三重校验 / 诊断（同 v2）=====
struct VerifyReport { compiles, ref_solution_passes, template_fails, diagnostics }
struct Diagnostic { code: Option<String>, message: String, severity, spans }

// ===== 错题本 / 画像 / 配置 / 用量（大体同 v2）=====
struct NotebookEntry { id, ts, error_code, concepts, minimal_repro, fix,
                        user_explanation, review: Option<ReviewRound>,
                        related_template }
struct ErrorProfile { by_code: HashMap<String, u32>,
                      by_concept: HashMap<String, ConceptState> }
struct ModelConfig { endpoint, api_key, model, context_len, think_mode,
                     prices, budget, editor: Option<String> }
struct UsageRecord { ts, model, input_tokens, output_tokens, cost, phase }
```

### 7.3 Agent 工具表
| 工具 | 输入 | 输出 |
|---|---|---|
| `verify_code(code, mode)` | 代码 + 模板/参考 | VerifyReport |
| `pick_template_by_concept(concept)` | 概念 id | 候选模板列表 |
| `pick_template_by_error(code)` | E0xxx（经反查索引映射概念） | 候选模板列表 |
| `instantiate_template(template_id, slots)` | 模板+槽位 | GeneratedExercise |
| `review_solution(template, user_code, test_results)` | 解答上下文 | SolutionReview |
| `check_constraints(code, constraints)` | 代码+约束 | 静态约束违例列表 |
| `make_explanation_quiz(diagnostics, concept)` | 真实诊断+概念 | 选择题选项 |
| `measure_solutions(variants)` | 多解代码 | ComparisonTable（实测） |
| `decide_follow_up(concept_state, signals)` | 掌握度+信号 | FollowUpDecision |
| `apply_edit_and_check(code, edit)` | 代码+改动 | 诊断 diff（借用实验室） |
| `query_notebook(concept_or_code)` | 概念/错误码 | 相似旧错 |
| `record_attempt / record_notebook` | 记录 | 更新画像 |

### 7.4 题目质量门（多层布尔，非分数）
1. 硬校验：三重通过（必过）
2. 规则过滤：概念 ≤2、行数 10–40、todo! 数 ≤2、测试直连概念
3. 约束自洽：参考解满足模板声明的全部静态约束（防"题自己都做不到"）
4. 用户反馈：尝试次数/放弃/完成时间/标记"太简单/太难/没意义"
   → 阈值规则降推荐优先级或降档

### 7.5 分层出题（M4.5 基线）

> 动机：模板库覆盖有边界，用户主题任意（如"Box<dyn Error> + ? 的错误
> 传播"可能无模板可配）。核心认识：**质量承诺来自质量门（三重校验 +
> 规则过滤 + 约束自洽），不来自模板本身**——模板只是输入先验。因此
> 生成的来源可以分层放宽，产出统一收口到同一质量门。

```
用户请求（任意主题）
 ├─ 第 1 级 模板直配（M3 已有）：概念/错误码/关键词命中 → 填槽
 │    成本≈0，离线可用；覆盖 = 模板库边界
 ├─ 第 2 级 模板改编：命中相近模板 → LLM 以它为参考骨架改写场景
 │    到用户主题（模板是"参考"不是"照搬"）
 └─ 第 3 级 自由生成：无相近模板 → LLM 按 ExerciseDraft 规格现场产题；
      rustc JSON 诊断回喂形成修复环（≤4 轮）
```

- **统一表示 `ExerciseDraft`**：{title, concepts(≤2，必须落在现有图谱，
  否则挂最近父节点并记 miss), error_codes, difficulty, constraints,
  body, tests, reference}。`Template = Draft + slots`（手写、预验证），
  三级共用 rule_filter / 三重校验 / 约束自洽，门代码只有一份。
- **修复环**：校验失败 → 把 rustc JSON 诊断（rendered 截断）+ 失败原因
  回喂 LLM 重出 JSON；重试耗尽明确报错。
- **前置硬化**：verifier 加超时（spawn + 轮询 + kill，防参考解死循环
  挂死 CLI）；自由生成默认追加 ban 约束（unsafe / std::process /
  std::fs）；prompt 强制 std-only（单文件 rustc 链不了外部 crate）、
  测试必须断言可观察行为。
- **成本**：第 3 级预计 3–5 次 LLM 调用/题；沿用预算拦截（R6）与逐条
  用量打印。
- **数据飞轮**：概念 miss 写 `~/.rustlings_adaptive/`（用户本地），
  人工据此扩模板/图谱；手写扩容（如 M3.1 错误处理模板）持续提高第 1 级
  命中率、降低 LLM 成本。远程 harvest 仍属 F12/P2 不动。
- **Agent 接口（M4）**：出题注册为工具，mode = auto（逐级回退）/
  matched / adapted / free，由 Agent 按对话上下文决定。

### 7.6 持久化
- repo 内（版本化、随代码演进）：`templates/*.toml`、`taxonomy/concepts.toml`
- 用户态：`~/.rustlings_adaptive/`：`sessions/*.json`（Agent 轨迹，R5）、
  `concept_state.json`、`error_profile.json`、`notebook.json`、`usage.json`
  （留白：后期改 sqlite）

### 7.7 未决项（留白）
- 概念图谱初版粒度与覆盖（目标 30–50 节点，从 8 道种子题 + 常见 E0xxx 反推）
- 解释选择题干扰项质量（手写种子 vs LLM 生成，需试跑评估）
- 约束静态检查初版实现深度（token/regex 是否够用，何时升 syn）
- 错题本检索 embedding 选型（本地 model vs API）— 延续 v2 留白
- 硬编码期望值检测的强度（探测测试的生成策略）

## 8. Vibe Coding 路线

> 原则：每个里程碑 = 一个可独立交付的最小增量 = 一个 AI session 的任务量；
> 先让 AI 定数据结构与接口 + 单测，再填实现；每步 `cargo check` / `cargo test`
> 绿了才继续；完成即更新 README 状态表 + 本文档对齐 + git commit。
> 时间对齐作业节点：**9.6 公开展示（需基本功能 + README + 链接开放）、
> 9.10 课堂展示**。今天 9.3。

| 日期 | 目标 |
|---|---|
| 9.3 | M1 + M2 + M3（含 M3.1 补丁，进度领先） |
| 9.4 | M4 |
| 9.5 | M4.5 分层出题 → **公开展示基线加固** |
| 9.6 | 发布展示帖（摘要 + 链接），余量机动 |
| 9.7 | M5 |
| 9.8 | M6（并完成试用 3 位同学作品） |
| 9.9 | M7（可砍）+ M8 前半 |
| 9.10 | M8 收尾 + 课堂展示 |

**M0 骨架（已完成）**：CLI + 8 种子练习 + r-a 子 crate + rustc --test 跑练习。

**M1 LLM 接入与计费（R1/R3/R6）** — 9.3 ✅
- 前置：把 main.rs 拆成 `cli/` + `exercise/` 模块（纯重构，行为不变）
- `config/`：.env/config.toml 加载 endpoint/key/model/价格/上下文/思考/预算
- `llm/`：reqwest + OpenAI 兼容 chat 接口 + usage 字段解析
- `usage/`：token/费用累计、展示、预算中断（超限返回 Err 中断调用链）
- 验收：`cargo run` 菜单里发一句 prompt，返回回复并打印 token/花费；
  预算中断有单测
- 实现备注：客户端用阻塞式 reqwest（REPL 简单）；chat 暂为一问一答，
  messages 多轮接口在 M4 随 Agent 环引入；新增 config.example.toml 样例，
  config.toml/.env 已 gitignore

**M2 验证器（R1 核心）** — 9.3~9.4 ✅
- `verifier/`：`rustc --error-format=json` 解析 → Diagnostic（码/消息/级别/
  span）；三重校验；测试失败断言（expected/actual）提取
- `constraints/`：初版静态检查（NoClone/NoUnwrap/IteratorOnly/MaxLines，
  token 级即可）
- 验收：单测——拿现有 8 题当 fixture：todo 模板必失败、参考解全绿、
  含 NoClone 的参考解若调 clone 则约束检查报违例
- 实现备注：诊断解析同时兼容 cargo 包裹（`reason` 字段）与纯 rustc
  （`$message_type` 字段）两种行格式；断言值提取为 left/right（libtest
  人类输出，含反引号剥离）；约束为 token 级 + 行注释剥离，已知局限：
  字符串字面量内的匹配可能误报（syn 级升级留待 F11）；种子题已按 §4.1
  新定位作为 fixture 使用（traits1 未解题=必失败样例，generics1 完整解
  =必过样例）；暂未接入 CLI（M3 随模板、M4 随 Agent 环接线）

**M3 模板库 + 概念图谱（出题基座）** — 9.4 ✅
- `templates/*.toml`：首批 10 个（所有权/借用/生命周期/trait/泛型各 2）
- `taxonomy/concepts.toml`：首批 30 节点（从种子题 + E0382/E0499/E0502/
  E0277/E0106/E0308 反推）
- `template/` + `taxonomy/`：加载、反查索引（码→概念）、填槽、规则过滤
- `generator/`：LLM 选模板 + 填槽 + 三重校验重试 + 自动写入 exercises/
  并接线 lib.rs
- 验收：对话中"来一道 trait 关联类型的题"→ 产出一道可开练的题
- 实现备注：实际落库 37 个概念节点（M3.1 补错误处理分支后；目标 30–50）；
  12 个模板（M3.1 补 Box<dyn Error>/? 与自定义错误类型 ×2）；`ConceptNode.templates`
  不手写，由 `link_templates` 从 `template.concepts` 反向填充（单一事实
  来源）；规则过滤口径——body 非空行数 10–40（题面注释计入）、`todo!`
  宏计数先剥离行注释（≤2）、概念 ≤2、槽位声明与占位符必须一一对应；
  §7.4 的"测试直连概念"暂未自动化（留给评审门）；生成器 LLM 填槽限制
  在模板声明的 values 白名单内（防幻觉值），失败静默回退默认填槽；重试
  至多 3 轮，LLM 只参与前两轮填槽（带上一轮失败反馈），第 3 轮纯轮转；
  未配置 Key 时全离线可用（默认填槽）；CLI 以 `g` 命令接线（输入主题 →
  生成 → 可立即开练），对话式入口待 M4 REPL；生成的练习落在
  `exercises/generated/`、接线写入 gitignored 的 `exercises/lib_generated.rs`
  （均为用户本地运行时产物，不入库，M3.2 定稿）

**M4 对话 REPL + Agent 环 + 进度/打断（R2/R4/R5 部分）** — 9.4~9.5 ✅
- `agent/`：工具注册 + Agent Loop + 轨迹记录
- `cli/`：对话 REPL 为**默认首屏**（见 §4.1 界面定位），做题界面降为
  子模式（对话出题触发或命令进入）；进度渲染（"验证 2/3"、"生成中…"）、
  Ctrl-C 打断、会话存 JSON 可回看；~~编辑器接入~~（已提前于 M3.2 完成，
  含 code --wait 等待提示与 GUI stdin 隔离）
- 交互反馈待办（来自试用）：清屏后长会话输出仍会堆积 → M4 评估
  分页/分屏/进度条等更友好的呈现；编辑器等待期间输入缓存问题已在
  M3.3 用提示缓解，REPL 输入层需根治
- 验收：启动即是对话；贴一段 E0308 报错 → 解释 → 出题 → 做题 → 通过，
  全程可打断，历史落盘可回看 → **此为 9.6 展示基线**
- 实现备注：`agent/` 三工具——`check_code`（rustc 真实诊断，30s 超时+
  打断轮询）、`generate_exercise`（复用 M3 生成管线，练习生成后 CLI 直接
  offer 进入做题）、`list_concepts`（概念图谱）；工具调用走 OpenAI 原生
  function calling，另留**文本协议兜底**（模型输出 `{"tool":...,
  "arguments":...}` JSON 即执行），弱 function-calling 端点也能跑通；
  上下文窗口 = 系统提示 + 最近 24 条消息（孤儿 tool 结果剔除、超长内容
  截断）；REPL 斜杠命令（/new /practice /generate /usage /config
  /sessions /exit），`a` 一问一答被 REPL 取代；**呈现取舍（试用反馈）**：
  彻底去掉清屏，输出如聊天滚动——`u` 被清屏吞掉的问题随之根治，分页/
  分屏评估后判定对 CLI 聊天流收益不足，不做；**输入缓存根治**：编辑器
  返回与打断返回后 `tcflush(TCIFLUSH)` 清空 tty 输入缓冲（libc）；
  多行粘贴用 ``` 围栏模式（两行 ``` 之间合为一条消息），避免逐行触
  发回合；R4 实现：spinner 线程（状态槽 + 耗时）+ ctrlc 全局标志，
  agent 回合跑在 worker 线程，打断 = 主线程放弃该回合（在途 HTTP 调
  用后台自然结束并照常记账），LLM 调用体内部不可打断（120s 超时上限）；
  R5 实现：每回合全量消息（含工具流量）落盘
  `~/.rustlings_adaptive/sessions/session_<ts>.json`，启动自动恢复最新
  会话，`/sessions <n>` 回看轨迹（角色标注 + 截断）；R6：每回合 footer
  汇总本回合与累计用量（phase：chat / generate）；练习发现根目录统一为
  `exercises/`（修掉一处会扫到 src/、target/ 的接线错误，冒烟中发现）

**M4.2 界面体验打磨（清屏策略 + 易用性）** — 9.5 ✅
- 动机（试用反馈）：M4 纯滚动导致长会话旧输出堆积淹没新内容；同时
  中文回复超宽破版、子模式缺页面感
- **清屏策略（核心取舍）**：清屏 ≠ 删历史——只清视口（`ESC[2J`+`ESC[H`，
  永不发 `3J`），终端回滚缓冲区原样保留，随时上滚可查。分面治理：
  对话流用"**视口重绘**"模式（每回合开始：清视口 → 页眉（会话/模型/
  累计花费/预算）→ 最近 3 条对话 dim 摘要 → 本回合输出），窗口永远
  只呈现当前语境；`/ui scroll` 可切回纯滚动（偏好写 config `[ui].mode`，
  旧配置缺省=view）；`/clear` 手动清、`/clear all` 深度清（唯一触发
  `3J` 的入口）；做题/配置为**交互页**（进入/每次修改后清视口整页
  重绘），用量/会话/帮助为**结果页**（保持滚动 + 页眉分隔）
- `cli/render.rs` 渲染基建（纯函数 + 单测）：unicode-width 宽字符
  折行（代码块 fence 内不折、续行缩进）、chat_tail 摘要、progress_bar、
  就近命令建议（编辑距离 ≤2）、ANSI 门控（非 tty 或 NO_COLOR 一律
  关闭，管道输出零 ANSI）
- 易用性：`/topics` 暴露概念图谱（agent 工具 list_concepts 的人用版）、
  未知命令就近提示、做题页 ✓/✗ 着色与进度条、编辑器返回自动重绘并
  重编译、首次运行引导行、回复按终端宽度折行
- 顺带修复：M3 `leftovers()` 的 never_loop 写法（clippy deny）
- 评估后不做：翻页器（与阻塞读键/Ctrl-C 冲突大收益低）、readline
  方向键历史（raw mode 与粘贴模式/打断机制纠缠，M8 后再评估）

**M4.3 MD 渲染 + 会话切换 + 输入加固** — 9.5 ✅
- **MD→ANSI**（`cli/md.rs`，手写子集 ~200 行不引库）：围栏代码块保真
  不折行、`#` 标题青粗、`-/*/+` 列表 → "• "、`>` 引用 `│` 前缀、
  `**粗体**`/行内码着色；折行复用宽字符逻辑；**前瞻扫描器**保证无
  闭合标记时字面输出（`3**4`、`2*3` 不吞字符）；管道输出保持源码。
  不做：MD 表格美化（按原样可读）、pulldown-cmark（重）
- **R5 补全（试用反馈"能否切回旧会话"）**：`/sessions load <n>` 切换
  到历史会话继续对话（切换前自动保存当前；window 裁剪直接适用），
  `/sessions export <n>` 导出全量轨迹为 `<id>.md`（顺带服务作业
  "AI 对话历史"交付物），Session 增 `title`（首条用户消息截 30 字，
  save 时惰性派生，旧文件缺字段兼容）
- **R3 收尾**：`context_len` 真正接线——窗口按 token 估算
  （ASCII≈4 字符/token、CJK≈1）裁到配置的 ¾，回复留 ¼ 余量；硬上限
  40 条消息双保险；`think_mode` 维持预留（绝大多数 OpenAI 兼容端点
  无统一开关参数，接入属各端点定制，M8 前不投入）
- **输入层加固（试用反馈两 bug）**：①"键入中文问题直接退出"根因=
  非 UTF-8 字节使 `read_line` 返回 Err 被当作 EOF（已复现：GBK 字节
  必现）——新 `cli/input.rs` raw-mode 行编辑器：无效字节 → U+FFFD +
  一次性编码提示，绝不退出；②"中文退格要按两次"根因=行律按字节擦除
  ——编辑器按**显示宽度**擦除（CJK 一次删净）、吞掉方向键等转义序
  列、Ctrl-C 取消本行（ISIG→EINTR→Interrupted）、Ctrl-D 空行才退
  出；非 tty 回退 `read_line`（Err 也不再当 EOF）；RAII 恢复 termios
- **崩溃面加固**：usage 锁全部 poison 容忍（`into_inner`）；worker
  线程崩溃从"静默"改为明确报错；`leftovers()` never_loop 已随 M4.2 修
- 明确不做：GBK/Big5 解码（提示用户切 UTF-8 即可）、readline 方向键
  历史（维持 M4.2 结论）
- 交接补丁：spinner 首帧先换行（不再吃掉刚回显的"你> …"输入行）、
  每帧追加 `ESC[0K`（状态文本变短不再残留旧字）——试用反馈"思考中
  xx 秒覆盖别的进度前缀"已修

**M5 前置定稿：对话内题目的组织 + 做题界面成熟化（插在 M4.5 前后执行）**
- 动机：生成的题散落 `exercises/generated/`，做题界面是"全库列表"，
  对话上下文与做题流脱节；8 道种子题混在列表里（定位是 fixture）
- **会话题架**：Session 增 `exercises: [{name, path, concepts,
  difficulty, source(对话生成|模板库), status(未做/失败n次/通过)}]`；
  `/practice` 顶部新增"本会话题目"分区（对话产出优先且有序），全库
  列表降为第二区
- **题目页成熟化**：单题卡片（概念标签/难度/约束/来源）；失败时
  rustc 错误码高亮；新增 **`[a] 问教练`**——把当前代码与真实诊断作为
  上下文带回对话（即 M5 评审门的自然入口，一次接线两用）
- **种子题 fixture 化**（提前自 M8）：`exercises/fixtures/` 目录或
  `.fixture` 标记，`discover` 默认过滤，`/practice all` 才显示全库
- **与 ProgressBus 衔接**：做题流状态（编译/运行/评审）并入统一进度
  事件模型（见"R4 进度事件模型"规划，M4.5/M5 落地时一并实现）

**M4.5 分层出题（F16 提级 + 模板扩容）** — 9.5
- 第 2 级 模板改编：相近模板为参考骨架，LLM 改写场景到用户主题
- 第 3 级 自由生成：ExerciseDraft JSON 现场产题 + rustc 诊断回喂修复环
- 统一 `ExerciseDraft` 表示，三级共用同一质量门（见 §7.5）
- 前置硬化：verifier 超时；自由生成默认 ban 约束；概念 miss log
- 验收：说"给一道 Box<dyn Error> 传播的题"（无直配模板时）→ 产出
  相关、可开练的题；三层各有演示路径
- 砍刀预案：第 3 级降级为仅模板改编或关闭入口（第 1 级永远保底）

**M5 评审门 + 交互式复盘（评环）** — 9.7
- `review/`：评审门（静态 + LLM JSON 评审 + 探测测试）；复盘四步
  （解释选择/输入校核 → 更优解挑战 → 对比表：机器实测 + LLM 双维度
  评审（§4.3 定稿）→ follow-up 决策）
- `usage/` 打 phase 标签（评审/复盘单独计费可见）
- 验收：故意交一个能过但不地道的解（手动循环 + 多余 clone）→ Agent
  提示更优解 → 再试 → 对比表（含 LLM 评维度与具体改法）→ 归档

**M6 双轨画像 + 错题本（记环，R5）** — 9.8
- `profile/`：信号入画像（错误码计数 + 概念 SM-2）；错题本 + 检索
  （先按码/概念，后加 embedding）；`store/` 落盘
- 验收：同概念错两次 → 主动推变式题；复盘后 SM-2 更新；stats 看图谱
  与花费

**M7 借用检查器假设实验室（招牌，P1）** — 9.9（滑期则砍为固定 demo 脚本）
- `borrowlab/`：apply_edit_and_check + 诊断 diff 呈现
- 验收：贴代码 + 假设改动，看 E0xxx 增量与解读

**M8 收尾** — 9.9~9.10
- README 定稿（编译/配置 endpoint/key/运行/演示用例）、集成测试、
  稳定演示脚本、设计文档与实现对齐、AI 开销明细表整理
- 种子题处置：把 8 道种子题 fixture 化（移出默认发现路径或标注），
  确保最终默认首屏是对话而非题库

### 8.1 砍刀预案（滑期时的降级顺序）
1. M7 borrowlab → 演示脚本假跑（展示优先级高于完整功能）
2. M5 复盘：选择题 → 只留自由输入；对比表 → 只留 clippy + 行数
3. M6 SM-2 → 降为概念计数器；错题本检索 → 只按错误码/概念精确匹配
4. F5 模板首批 10 → 6 个；约束只留 NoClone/NoUnwrap
5. M4.5 自由生成 → 降级为仅模板改编或关闭入口（第 1 级模板直配保底）
6. 反向不可砍：R1–R6 六项硬要求、三重校验、评审门（本项目的辨识度所在）

### 8.2 每个 session 的交接纪律
1. 新 session 开场：读 README 状态表 + 本文档 §8 对应里程碑（已写入 AGENTS.md）
2. 一次只做一个里程碑，不跨里程碑
3. 先接口与单测、后实现；每步编译/测试绿
4. 里程碑完成：更新 README 状态表 → git commit（信息格式 `M<n>: 摘要`）
5. 实现与文档冲突时，改代码同时改本文档

---
> 对齐原则：实现过程中想法会变，改代码同时更新本文档。v3 以"小片段主义
> + 双轨锚定 + 评审门 + 交互式复盘"为基线。
